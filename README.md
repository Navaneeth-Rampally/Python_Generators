# Python_Generators
Generators- is a special type of function that allows you to create an iterator. 
->It uses the keyword "yield". 
->when it hits yield, it saves it's state (local variables and code position) and pauses. 
-> The main aim of generators is increases efficiency, specifically regarding memory and speed.